# Legacy manual runtime helper

This directory is retained only as a fallback/debugging aid from v0.8.x.

**v0.11.3 no longer requires `traceFlowGraph(...)` calls.** Use the included
`instrumentation/` Gradle build and `com.oskiapps.flowgraph.instrumentation` plugin instead.

<idea-plugin>
    <id>dev.flowgraph.analysisapi.prototype</id>
    <name>Flow Graph</name>
    <vendor>Flow Graph</vendor>

    <description><![CDATA[
      Visualize Kotlin Flow/StateFlow/SharedFlow and Compose state propagation with K2 Analysis API, live runtime tracing, Compose inspection, UI-interaction markers, and a scrub-able execution timeline.
    ]]></description>

    <depends>com.intellij.modules.platform</depends>
    <depends>org.jetbrains.kotlin</depends>
    <depends>com.intellij.java</depends>

    <!-- Android Studio / IntelliJ <= 2026.1 requires plugins that depend on
         org.jetbrains.kotlin to explicitly opt into K2 mode. The analyzer
         uses the Kotlin Analysis API and is intended for K2. -->
    <extensions defaultExtensionNs="org.jetbrains.kotlin">
        <!--suppress PluginXmlValidity -->
        <supportsKotlinPluginMode supportsK1="false" supportsK2="true"/>
    </extensions>

    <extensions defaultExtensionNs="com.intellij">
        <toolWindow id="Flow Graph"
                    anchor="right"
                    factoryClass="dev.flowgraph.ui.FlowGraphToolWindowFactory"
                    canCloseContents="false"/>
        <projectService serviceImplementation="dev.flowgraph.service.FlowGraphProjectService"/>
        <projectService serviceImplementation="dev.flowgraph.service.LiveTraceService"/>
        <projectService serviceImplementation="dev.flowgraph.service.ComposeRenderService"/>
    </extensions>

    <actions>
        <action id="FlowGraph.ShowForCaret"
                class="dev.flowgraph.action.ShowFlowGraphAction"
                text="Show Flow Graph"
                description="Resolve the Flow/StateFlow at the caret with Kotlin Analysis API and visualize its propagation">
            <add-to-group group-id="EditorPopupMenu" anchor="last"/>
            <keyboard-shortcut keymap="$default" first-keystroke="ctrl alt G"/>
        </action>
    </actions>
</idea-plugin>
